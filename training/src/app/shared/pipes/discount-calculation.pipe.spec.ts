import { DiscountCalculationPipe } from './discount-calculation.pipe';

describe('DiscountCalculationPipe', () => {
  it('create an instance', () => {
    const pipe = new DiscountCalculationPipe();
    expect(pipe).toBeTruthy();
  });
});
